# 本地凭据 / 配置 — 复制本文件为 .env.local 并填入真实值
# .env.local 已加入 .gitignore，绝不会被提交
#
# 加载时机：
#   - Web 后端：src/index.ts 启动时
#   - Electron：electron/main.ts 启动时
#   - 勘察脚本：各 inspect-*.ts 自行加载
# 加载方式：dotenv.config({ path: '.env.local', override: false })
#   注意 override: false —— 真实环境变量（CI / shell `set`）优先于本文件

# ── BY 源（std.samr.gov.cn）─────────────────────────────
# 默认账号已 hard-code 在 src/sources/by/by-adapter.ts（leiming/888888），
# 想换号在这里覆盖
# BY_USERNAME=
# BY_PASSWORD=
# BY_DEPT_ID=

# ── LABR 源（labr.cc）──────────────────────────────────
# 必填，未配置则 labr 下载链路直接报错
# LABR_USERNAME=
# LABR_PASSWORD=

# ── SPC 源（spc.org.cn）─────────────────────────────────
# adapter 默认走「admin 面板手动粘贴 Cookie」路径（submitlogin 需要图形验证码）。
# SPC_USERNAME / SPC_PASSWORD 暂仅用于勘察脚本 + 未来 OCR-自动登录 polish；
# 当前 MVP 不依赖它们，可以留空。
# SPC_USERNAME=
# SPC_PASSWORD=
